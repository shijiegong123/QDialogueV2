# 小车端 PTT 配置文件 (config.txt)

# 平板端的 IP 地址 (必填)
peer_ip=192.168.1.100

# 小车端本地监听的 UDP 端口 (必填，范围 1024-65535)
local_port=9001

# 平板端监听的 UDP 端口 (必填，范围 1024-65535)
peer_port=9000

# 日志文件路径 (选填，不填或留空则仅输出到终端)
#log_file=/var/log/car_ptt.log
